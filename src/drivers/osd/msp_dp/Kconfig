menuconfig DRIVERS_OSD_MSP_DP
	bool "msp_dp"
	default n
	---help---
		Enable support for msp_dp

menuconfig USER_MSP_DP
	bool "msp_dp running as userspace module"
	default y
	depends on BOARD_PROTECTED && DRIVERS_OSD_MSP_DP
	---help---
		Put msp_dp in userspace memory
